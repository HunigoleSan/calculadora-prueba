/* 
    ¿Para que sirve el comando if ?
        #Define una condición
        #Permite evaluar una condición
        #Ejecuta cuando la condición es verdera

    ¿Para que sirve el else?
        #Es otra condición
        #Es un bloque y condición alternativo, en caso el if resulte 'falso'
    
    ¿Para que el else if?
        #Tambien es una condición si es que los dos no dan ✅ ❌
        #Permite evaluar otra condición alterna, en caso el if resulte 'falso'
*/

    /*
        El objetivo del if elegir la respuesta correcta para realizar
        las acciones
    */
    

    if(_damaris_){
        console.log("Soy el if - ejecuto cuando soy verdadero")
    }else if(_segunda_condicion){
        console.log("Soy el else if - ejecuto cuando sea")
    }else{
        console.log("Soy el bloque else - ejecuto cuando soy falso")
    }

/* 
    SWITCH
    #es lo mismo que if, pero solo permite condiciones simples
*/

